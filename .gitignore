# Login Screen Django

Uma tela de autenticação moderna e responsiva construída com **Python** e **Django**, perfeita como ponto de partida para qualquer aplicação web que exija login de usuário.

## 📝 Descrição

Este projeto apresenta um **login screen** elegante e funcional, com foco em:

- **Segurança**: utiliza o modelo `User` nativo do Django, `django.contrib.auth` e proteção contra CSRF.  
- **Usabilidade**: feedback visual em hover, validação em tempo real via JavaScript puro e placeholders claros.  
- **Responsividade**: layout baseado em **Bootstrap 5** com Grid, Flexbox e media queries para ajuste em todos os tamanhos de tela.  
- **Manutenção**: estilos organizados em **SASS** e componentes HTML reaproveitáveis via Django Templates (herança e includes).

## 🚀 Tecnologias

- **Backend**: Django (views genéricas de autenticação, sessões, CSRF)  
- **Frontend**: HTML5, CSS3, Bootstrap 5, SASS  
- **Scripts**: JavaScript vanilla para validação de campos  
- **Templates**: Django Templates com blocos e includes  

## ⚙️ Instalação

1. Clone o repositório  
   ```bash
   git clone https://github.com/seu-usuario/login-screen-django.git
   cd login-screen-django
